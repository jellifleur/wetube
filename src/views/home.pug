extends layouts/main.pug
include mixins/videoBlock


block content
  h1.home__title WETUBE — Life Is Beautiful.
    //span Thank you, Nikko. Your lecture has helped me a lot.
  .home-videos
    each item in videos
      +videoBlock({
          id:item.id,
          title:item.title,
          views:item.views,
          videoFile:item.fileUrl,
          creator:item.creator.name
      })